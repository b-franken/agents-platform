## Summary

<!-- Brief description of what this PR does -->

## Changes

-

## Testing

- [ ] `uv run ruff check .` passes
- [ ] `uv run pytest tests/` passes
- [ ] `uv run python -m agent_core.validate` passes
- [ ] Tested locally with `uv run --package router-agent python -m router.main`
